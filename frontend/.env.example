# Pi base URL for live feed, telemetry, and API (aiPiCam.py Flask port 5000).
# Default in code: http://192.168.50.1:5000 (HorizonAir network). Set if Pi has a different IP on your LAN.
#
# VITE_PI_BASE_URL=http://192.168.50.1:5000
